---
import Layout from '@/layouts/Layout.astro';
import '@/styles/globals.css';
---

<Layout title="Task Management - Colby Remodel Agent">
  <main class="h-screen flex flex-col">
    <div class="border-b p-4">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold">Task Management</h1>
          <p class="text-muted-foreground">Kanban board for renovation task tracking</p>
        </div>
        <div class="flex gap-4">
          <a href="/admin/timeline" class="text-sm text-muted-foreground hover:text-foreground underline">
            Timeline View
          </a>
          <a href="/admin" class="text-sm text-muted-foreground hover:text-foreground underline">
            Dashboard
          </a>
        </div>
      </div>
    </div>
    
    <div class="flex-1 overflow-hidden">
      <div id="task-kanban-container" class="h-full"></div>
    </div>
  </main>
</Layout>

<script>
  import { jsx as _jsx } from "react/jsx-runtime";
  import { createRoot } from "react-dom/client";
  import TaskKanban from "@/components/admin/TaskKanban.tsx";

  const container = document.getElementById("task-kanban-container");
  if (container) {
    const root = createRoot(container);
    root.render(_jsx(TaskKanban, {}));
  }
</script>
